name: Update portfolio data

on:
  schedule:
    # Hourly. GitHub may delay/skip scheduled runs under load, so this is
    # "best effort, a few+ times a day" rather than guaranteed every hour.
    - cron: '0 * * * *'
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: update-portfolio
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Fetch prices and update data
        run: node scripts/update-portfolio.mjs

      - name: Commit updated data
        run: |
          git config user.name "portfolio-bot"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/portfolio.json data/history.json
          if git diff --cached --quiet; then
            echo "No data changes."
          else
            git commit -m "chore: update portfolio data [skip ci]"
            git push
          fi
